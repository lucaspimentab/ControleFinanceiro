#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <iostream>
#include <limits>

bool validarNome(const std::string& nome);
bool validarSalario(const std::string& salario);
void limparEntrada();

#endif // UTILS_HPP